export PYTHONPATH=$PYTHONPATH:../
export VIRTUAL_ENV="$PWD/ur_env"
PATH_add "$VIRTUAL_ENV/bin"   # 这是 direnv stdlib 的函数，等价于把 bin 加到 PATH 前面

# Load environment variables from .env file
dotenv

echo "# Usage: python ur_vacancy_monitor.py"
